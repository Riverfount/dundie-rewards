[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q -vv --forked"
testpaths = [
    "tests",
    "integration",
]

[tool.flake8]
exclude = ['.venv', 'build']
max-line-length = 120

[tool.isort]
src_paths = ['dundie', 'tests', 'integration']
multi_line_output = 3
